import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  evolutionChain: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
    marginTop: 7,
    marginBottom: 7,
  },
  evolution: {
    alignItems: 'center',
  },
  evolutionVector: {
    alignItems: 'center',
  },
  level: {
    fontSize: 12,
    marginTop: 5,
  },
});
